const metadata = {
  title: "Working Experience",
  description: "Manage Working Experience",
};

export default function WorkingExperienceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
