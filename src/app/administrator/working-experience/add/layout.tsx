export const metadata = {
  title: "Add new working experience",
  description: "Add new working experience to show to the Website",
};

const WorkingExperienceAddLayout = ({
  children,
}: {
  children: React.ReactNode;
}) => {
  return <div className="mt-10">{children}</div>;
};

export default WorkingExperienceAddLayout;
