const metadata = {
  title: "Working Experience Detail",
  description: "Manage the existing working experience !",
};

export default function WorkingExperienceDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
